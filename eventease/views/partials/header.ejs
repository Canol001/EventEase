<!DOCTYPE html>
<html lang="en">
<head>
  <% const hasEvent = typeof event !== 'undefined'; %>
  
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  
  <!-- Dynamic Title -->
  <title><%= hasEvent ? event.name + ' | EventEase' : 'EventEase' %></title>
  
  <!-- Meta Description -->
  <meta name="description" content="<%= hasEvent ? ('Join us for ' + event.name + ' at ' + event.location + ' on ' + event.date + '! Book your seat now.') : 'Discover and book seats for amazing events on EventEase.' %>">
  
  <!-- Open Graph Meta Tags for Social Sharing -->
  <meta property="og:title" content="<%= hasEvent ? event.name + ' | EventEase' : 'EventEase' %>">
  <meta property="og:description" content="<%= hasEvent ? ('Join us for ' + event.name + ' at ' + event.location + ' on ' + event.date + '! Book your seat now.') : 'Discover and book seats for amazing events on EventEase.' %>">
  <meta property="og:image" content="<%= hasEvent ? ('https://eventease-as8q.onrender.com/uploads/' + event.image) : 'https://eventease-as8q.onrender.com/default-share-image.jpg' %>">
  <meta property="og:url" content="https://eventease-as8q.onrender.com/">
  <meta property="og:type" content="website">
  
  <!-- Tailwind CSS CDN -->
  <script src="https://cdn.tailwindcss.com"></script>
  <script>
    tailwind.config = {
      theme: {
        extend: {
          colors: {
            primary: '#3b82f6', // Blue accent
            darkBg: '#1f2937',  // Dark background
            cardBg: '#374151',  // Card background
            accentYellow: '#fef08a', // Yellow for highlights
          },
          fontFamily: {
            futuristic: ['Orbitron', 'sans-serif'],
          },
        },
      },
    };
  </script>
  
  <!-- Google Fonts (Optional) -->
  <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@400;700&display=swap" rel="stylesheet">
</head>

<body class="bg-darkBg text-white min-h-screen">
  <header class="bg-darkBg border-b border-gray-700 p-4">
    <div class="container mx-auto flex justify-between items-center">
      <h1 class="text-3xl font-futuristic text-white">EventEase</h1>
      <nav class="space-x-6">
        <a href="/" class="text-white hover:text-primary transition">Home</a>
        <a href="/events/create" class="text-white hover:text-primary transition">Create Event</a>
      </nav>
    </div>
  </header>
  <main class="container mx-auto py-8">